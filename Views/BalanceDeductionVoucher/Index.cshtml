@{
    ViewData["Title"] = "سند خصم رصيد";
    ViewData["Direction"] = "rtl";
}

@section styles
{
    <!-- jstree css -->
    <link href="~/plugins/jstree/style.min.css" rel="stylesheet" type="text/css">
    <!-- date range picker css -->
    <link href="~/plugins/daterangepicker/daterangepicker.css" rel="stylesheet" type="text/css">
    <!-- flatpickr css -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <link rel="stylesheet" type="text/css" href="https://npmcdn.com/flatpickr/dist/themes/material_blue.css">
    <!-- datatables css - now using CDN in layout -->
    
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600&display=swap" rel="stylesheet">

    <!-- UI Polish CSS -->
    <link href="~/css/ui-polish.css" rel="stylesheet" type="text/css">
    
    <!-- Voucher Modal CSS -->
    <link href="~/css/voucher-modal.css" rel="stylesheet" type="text/css">

    <style>
        body[dir="rtl"] * {
            font-family: "Cairo", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
        }
        
        /* تعديل الجدول لإزالة المسافة بين الرأس والمحتوى */
        .table-bordered thead th {
            border-bottom-width: 1px;
        }
        
        .table-bordered tbody tr:first-child td {
            border-top: none;
        }
        
        .table {
            margin-bottom: 0;
        }
        
        /* Select all checkboxes styling */
        .form-check-input:checked {
            background-color: #1ab394;
            border-color: #1ab394;
        }
        
        /* Alert styling for better visibility */
        .alert-warning {
            background-color: #fff3cd;
            border-color: #ffeaa7;
            color: #856404;
        }
        
        .alert-danger {
            background-color: #f8d7da;
            border-color: #f5c6cb;
            color: #721c24;
        }
        
        /* Dark mode alert adjustments */
        [data-bs-theme="dark"] .alert-warning {
            background-color: #664d03;
            border-color: #7d5f04;
            color: #ffecb5;
        }
        
        [data-bs-theme="dark"] .alert-danger {
            background-color: #721c24;
            border-color: #842029;
            color: #f8d7da;
        }
        
        /* Calendar icon positioning */
        .date-input-wrapper {
            position: relative;
            width: 100%;
        }
        
        .calendar-icon {
            position: absolute;
            left: 0.75rem;
            top: 50%;
            transform: translateY(-50%);
            pointer-events: none;
            z-index: 2;
            color: #6b7280;
        }
        
        [data-bs-theme="dark"] .calendar-icon {
            color: #9ca3af;
        }
        
        /* اجعل التقويم فوق المودال */
        .flatpickr-calendar { z-index: 200000 !important; }
        
        /* Popup menu card styles */
        #popupMenuModal .card {
            transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
            border: 1px solid rgba(0, 0, 0, 0.125);
        }
        
        #popupMenuModal .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
        }
        
        #popupMenuModal .card i {
            font-size: 2rem;
        }
        
        #popupMenuModal .card .btn {
            transition: all 0.2s ease-in-out;
        }
        
        #popupMenuModal .card .btn:hover {
            transform: scale(1.05);
        }
        
        /* Modal header styling - using project's primary color */
        .modal-header {
            background-color: #1ab394; /* Project's primary color */
            color: white;
            border-bottom: 1px solid #16987e;
        }
        
        /* Modal title styling */
        .modal-header .modal-title {
            font-weight: 500;
            color: white;
        }
        
        /* Close button styling for modal headers */
        .modal-header .btn-close {
            filter: invert(1) grayscale(100%) brightness(200%);
        }
        
        /* Modal footer styling - using a lighter shade */
        .modal-footer {
            background-color: #f0f8f8; /* Light teal background */
            border-top: 1px solid #d1e8e8;
        }
        
        /* Primary buttons in modals */
        .modal-footer .btn-primary {
            background-color: #1ab394;
            border-color: #1ab394;
        }
        
        .modal-footer .btn-primary:hover {
            background-color: #16987e;
            border-color: #16987e;
        }
        
        /* View account modal specific styles */
        #viewCustomerModal .form-control-plaintext {
            background-color: #f8f9fa;
            border: 1px solid #dee2e6;
            min-height: 38px;
            display: flex;
            align-items: center;
            font-weight: 500;
        }
        
        /* Dark mode adjustments for view modal */
        [data-bs-theme="dark"] #viewCustomerModal .form-control-plaintext {
            background-color: #272832;
            border-color: #293036;
            color: #aab8c5;
        }
        
        /* Consistent button styling across all modals */
        .btn-info {
            background-color: #23c6c8;
            border-color: #23c6c8;
        }
        
        .btn-info:hover {
            background-color: #1ea8aa;
            border-color: #1ea8aa;
        }
        
        /* Dark mode button adjustments */
        [data-bs-theme="dark"] .btn-info {
            background-color: #23c6c8;
            border-color: #23c6c8;
        }
        
        [data-bs-theme="dark"] .btn-info:hover {
            background-color: #1ea8aa;
            border-color: #1ea8aa;
        }
        
        /* Tree view container */
        #trusted-customers-tree {
            min-height: 400px;
            border: 1px solid #ddd;
            border-radius: 4px;
            padding: 10px;
        }
        
        /* RTL adjustments for input group */
        [dir="rtl"] .input-group > :not(:first-child):not(.dropdown-menu):not(.valid-tooltip):not(.valid-feedback):not(.invalid-tooltip):not(.invalid-feedback) {
            margin-left: -1px;
            border-top-left-radius: 0.375rem;
            border-bottom-left-radius: 0.375rem;
        }
        
        [dir="rtl"] .input-group > :not(:last-child):not(.dropdown-menu):not(.valid-tooltip):not(.valid-feedback):not(.invalid-tooltip):not(.invalid-feedback) {
            border-top-right-radius: 0.375rem;
            border-bottom-right-radius: 0.375rem;
        }
        
        /* RTL adjustments for date picker */
        [dir="rtl"] .daterangepicker {
            direction: rtl;
            text-align: right;
        }
        
        [dir="rtl"] .daterangepicker .calendar-table {
            direction: rtl;
        }
        
        [dir="rtl"] .daterangepicker .drp-calendar.left {
            float: right;
        }
        
        [dir="rtl"] .daterangepicker .drp-calendar.right {
            float: left;
        }
        
        [dir="rtl"] .daterangepicker .ranges ul {
            direction: rtl;
            text-align: right;
        }
        
        [dir="rtl"] .daterangepicker .ranges li {
            text-align: right;
        }
        
        [dir="rtl"] .daterangepicker .drp-buttons {
            direction: rtl;
            text-align: left;
        }
        
        /* Custom flatpickr calendar styling to match React component */
        .flatpickr-calendar {
            direction: rtl;
            display: block !important; /* Ensure calendar is always displayed */
        }
        
        .flatpickr-calendar .flatpickr-months {
            background: #111827;
            border-bottom: 1px solid #374151;
        }
        
        .flatpickr-calendar .flatpickr-weekdays {
            background: #111827;
            border-bottom: 1px solid #374151;
        }
        
        .flatpickr-calendar .flatpickr-weekday {
            color: #e5e7eb;
        }
        
        .flatpickr-calendar .flatpickr-day {
            color: #e5e7eb;
            border-radius: 8px;
        }
        
        .flatpickr-calendar .flatpickr-day.today {
            border-color: #60a5fa;
        }
        
        .flatpickr-calendar .flatpickr-day.selected {
            background: #2563eb;
            border-color: #2563eb;
            color: #fff;
        }
        
        .flatpickr-calendar .flatpickr-day:hover {
            background: #3b82f6;
            border-color: #3b82f6;
            color: #fff;
        }
        
        /* Light mode flatpickr styling */
        .flatpickr-calendar:not(.dark) {
            background: #ffffff;
            border: 1px solid #d1d5db;
        }
        
        .flatpickr-calendar:not(.dark) .flatpickr-months {
            background: #f3f4f6;
            border-bottom: 1px solid #d1d5db;
        }
        
        .flatpickr-calendar:not(.dark) .flatpickr-weekdays {
            background: #f3f4f6;
            border-bottom: 1px solid #d1d5db;
        }
        
        .flatpickr-calendar:not(.dark) .flatpickr-weekday {
            color: #4b5563;
        }
        
        .flatpickr-calendar:not(.dark) .flatpickr-day {
            color: #374151;
        }
        
        .flatpickr-calendar:not(.dark) .flatpickr-day.today {
            border-color: #60a5fa;
        }
        
        .flatpickr-calendar:not(.dark) .flatpickr-day.selected {
            background: #2563eb;
            border-color: #2563eb;
            color: #fff;
        }
        
        .flatpickr-calendar:not(.dark) .flatpickr-day:hover {
            background: #3b82f6;
            border-color: #3b82f6;
            color: #fff;
        }
        
        /* Dark mode flatpickr styling */
        .dark .flatpickr-calendar {
            background: #0a0a0a;
            border-color: #3f3f46;
            color: #e5e7eb;
        }
        
        .dark .flatpickr-calendar .flatpickr-months {
            background: #111827;
            border-bottom: 1px solid #374151;
        }
        
        .dark .flatpickr-calendar .flatpickr-weekdays {
            background: #111827;
            border-bottom: 1px solid #374151;
        }
        
        .dark .flatpickr-calendar .flatpickr-weekday {
            color: #e5e7eb;
        }
        
        .dark .flatpickr-calendar .flatpickr-day {
            color: #e5e7eb;
            border-radius: 8px;
        }
        
        .dark .flatpickr-calendar .flatpickr-day.today {
            border-color: #60a5fa;
        }
        
        .dark .flatpickr-calendar .flatpickr-day.selected,
        .dark .flatpickr-calendar .flatpickr-day:hover {
            background: #2563eb;
            border-color: #2563eb;
            color: #fff;
        }
        
        /* Enhanced dark mode styling for balance deduction modal */
        [data-bs-theme="dark"] #balanceDeductionModal .card {
            background-color: #1e1f27;
            border-color: #293036;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .card-header {
            background-color: #272832;
            border-color: #293036;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .card-body {
            background-color: #1e1f27;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .form-label {
            color: #aab8c5;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .form-control,
        [data-bs-theme="dark"] #balanceDeductionModal .form-select {
            background-color: #272832;
            border-color: #293036;
            color: #aab8c5;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .form-control:focus,
        [data-bs-theme="dark"] #balanceDeductionModal .form-select:focus {
            background-color: #272832;
            border-color: #1ab394;
            color: #aab8c5;
            box-shadow: 0 0 0 0.25rem rgba(26, 179, 148, 0.25);
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .form-control::placeholder {
            color: #8391a2;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .input-group-text {
            background-color: #272832;
            border-color: #293036;
            color: #aab8c5;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .date-picker-input {
            background-color: #272832;
            border-color: #293036;
            color: #aab8c5;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .date-picker-input:focus {
            background-color: #272832;
            border-color: #1ab394;
            color: #aab8c5;
            box-shadow: 0 0 0 0.25rem rgba(26, 179, 148, 0.25);
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .is-invalid {
            border-color: #f499a3 !important;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .is-invalid:focus {
            box-shadow: 0 0 0 0.25rem rgba(244, 153, 163, 0.25) !important;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .modal-header {
            background-color: #1ab394;
            border-color: #16987e;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .modal-footer {
            background-color: #272832;
            border-color: #293036;
        }
        
        [data-bs-theme="dark"] .text-muted {
            color: #8391a2 !important;
        }
        
        [data-bs-theme="dark"] .text-danger {
            color: #f499a3 !important;
        }
        
        /* Additional dark mode styling for form elements in balance deduction modal */
        [data-bs-theme="dark"] #balanceDeductionModal .border {
            border-color: #293036 !important;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .bg-white\/70 {
            background-color: rgba(39, 40, 50, 0.7) !important;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .border-slate-300 {
            border-color: #293036 !important;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .text-gray-500 {
            color: #8391a2 !important;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .text-gray-900 {
            color: #aab8c5 !important;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .border-gray-300 {
            border-color: #293036 !important;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .bg-white {
            background-color: #272832 !important;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .placeholder-gray-400::-webkit-input-placeholder {
            color: #8391a2 !important;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .placeholder-gray-400::-moz-placeholder {
            color: #8391a2 !important;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .placeholder-gray-400:-ms-input-placeholder {
            color: #8391a2 !important;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .placeholder-gray-400::-ms-input-placeholder {
            color: #8391a2 !important;
        }
        
        [data-bs-theme="dark"] #balanceDeductionModal .placeholder-gray-400::placeholder {
            color: #8391a2 !important;
        }
        
        /* Light mode styling for edit modal form controls */
        #editCustomerModal .form-control,
        #editCustomerModal .form-select {
            background-color: #ffffff;
            border-color: #d1d5db;
            color: #374151;
        }
        
        #editCustomerModal .form-control:focus,
        #editCustomerModal .form-select:focus {
            background-color: #ffffff;
            border-color: #1ab394;
            color: #374151;
            box-shadow: 0 0 0 0.25rem rgba(26, 179, 148, 0.25);
        }
        
        #editCustomerModal .form-control::placeholder {
            color: #9ca3af;
        }
        
        #editCustomerModal .form-label {
            color: #4b5563;
        }
        
        /* Dark mode styling for edit modal form controls */
        [data-bs-theme="dark"] #editCustomerModal .form-control,
        [data-bs-theme="dark"] #editCustomerModal .form-select {
            background-color: #272832;
            border-color: #293036;
            color: #aab8c5;
        }
        
        [data-bs-theme="dark"] #editCustomerModal .form-control:focus,
        [data-bs-theme="dark"] #editCustomerModal .form-select:focus {
            background-color: #272832;
            border-color: #1ab394;
            color: #aab8c5;
            box-shadow: 0 0 0 0.25rem rgba(26, 179, 148, 0.25);
        }
        
        [data-bs-theme="dark"] #editCustomerModal .form-control::placeholder {
            color: #8391a2;
        }
        
        [data-bs-theme="dark"] #editCustomerModal .form-label {
            color: #aab8c5;
        }
        
        [data-bs-theme="dark"] #editCustomerModal .text-muted {
            color: #8391a2 !important;
        }
        
        [data-bs-theme="dark"] #editCustomerModal .text-danger {
            color: #f499a3 !important;
        }
        
        /* Light mode table styles */
        .table-dark-card {
            background: #fff;
            border-radius: 10px;
            overflow: hidden;
            border: 1px solid #e5e7eb;
            box-shadow: 0 4px 20px rgba(0,0,0,.05);
        }

        .table-dark-card table {
            width: 100%;
            border-collapse: collapse;
            color: #374151;
            font-size: 14px;
        }

        .table-dark-card thead {
            background: linear-gradient(180deg, #f3f4f6, #e5e7eb);
        }
        
        .table-dark-card thead th {
            padding: 12px 14px;
            text-align: center;
            font-weight: 600;
            color: #111827;
            border-bottom: 1px solid #d1d5db;
            white-space: nowrap;
        }

        .table-dark-card tbody tr {
            background: #fff;
            transition: background 0.2s;
        }
        
        .table-dark-card tbody tr:nth-child(even) {
            background: #f9fafb;
        }
        
        .table-dark-card tbody tr:hover {
            background: #f3f4f6;
        }
        
        .table-dark-card td {
            padding: 12px 14px;
            text-align: center;
            border-bottom: 1px solid #e5e7eb;
        }

        .btn-more {
            border: 1px solid #d1d5db;
            background: transparent;
            color: #3b82f6;
            padding: 4px 10px;
            border-radius: 6px;
            font-size: 13px;
            cursor: pointer;
        }
        
        .btn-more:hover {
            background: #2563eb;
            color: #fff;
            border-color: #2563eb;
        }

        /* Dark mode table styles */
        [data-bs-theme="dark"] .table-dark-card {
            background: #111827;
            border: 1px solid #1f2937;
            box-shadow: 0 4px 20px rgba(0,0,0,.4);
        }

        [data-bs-theme="dark"] .table-dark-card table {
            color: #e5e7eb;
        }

        [data-bs-theme="dark"] .table-dark-card thead {
            background: linear-gradient(180deg, #1e293b, #111827);
        }
        
        [data-bs-theme="dark"] .table-dark-card thead th {
            color: #f9fafb;
            border-bottom: 1px solid #374151;
        }

        [data-bs-theme="dark"] .table-dark-card tbody tr {
            background: #111827;
        }
        
        [data-bs-theme="dark"] .table-dark-card tbody tr:nth-child(even) {
            background: #1a2234;
        }
        
        [data-bs-theme="dark"] .table-dark-card tbody tr:hover {
            background: #1f2937;
        }
        
        [data-bs-theme="dark"] .table-dark-card td {
            border-bottom: 1px solid #1f2937;
        }

        [data-bs-theme="dark"] .btn-more {
            border: 1px solid #374151;
            color: #93c5fd;
        }
        
        [data-bs-theme="dark"] .btn-more:hover {
            background: #2563eb;
            color: #fff;
            border-color: #2563eb;
        }

        /* Light mode pagination */
        .pagination-dark {
            display: flex;
            justify-content: center;
            margin-top: 12px;
            gap: 4px;
        }
        
        .pagination-dark button {
            background: #f9fafb;
            border: 1px solid #e5e7eb;
            color: #111827;
            padding: 6px 12px;
            border-radius: 6px;
            cursor: pointer;
        }
        
        .pagination-dark button.active {
            background: #10b981;
            border-color: #10b981;
            color: #fff;
        }
        
        .pagination-dark button:hover:not(.active) {
            background: #2563eb;
            border-color: #2563eb;
            color: #fff;
        }

        /* Dark mode pagination */
        [data-bs-theme="dark"] .pagination-dark button {
            background: #1e293b;
            border: 1px solid #374151;
            color: #f9fafb;
        }
        
        [data-bs-theme="dark"] .pagination-dark button.active {
            background: #10b981;
            border-color: #10b981;
        }
        
        [data-bs-theme="dark"] .pagination-dark button:hover:not(.active) {
            background: #2563eb;
            border-color: #2563eb;
        }

        /* اجعل التقويم فوق أي عناصر أخرى */
        .flatpickr-calendar{z-index:200000 !important;direction:ltr}
        /* لو واجهت الصفحة RTL وأردت تقويم RTL فعّل التالي: */
        [dir="rtl"] .flatpickr-calendar{direction:rtl}

        [dir="rtl"] .flatpickr-calendar{direction:rtl}
        
        /* Ensure Flatpickr calendar is visible and properly positioned */
        .flatpickr-calendar.open {
            display: block !important;
            visibility: visible !important;
        }
        
        /* Additional positioning for modal calendars */
        .modal .flatpickr-calendar {
            position: absolute !important;
        }
    </style>
}

<!-- Filter toggle button -->
<div class="mb-2">
    <button id="toggleFilters" type="button" class="btn btn-primary">
        <i class="ti ti-filter-off me-1"></i><span id="toggleFiltersText">إظهار الفلترة</span>
    </button>
</div>

<!-- Filter section -->
<div class="card mb-3" id="filterSection" style="display: none;">
    <div class="card-body p-2">
        <!-- Filter row -->
        <div class="row g-2 mb-2">
            <div class="col">
                <input type="text" class="form-control form-control-sm" placeholder="القيمة" id="filter-value">
            </div>
            <div class="col">
                <input type="text" class="form-control form-control-sm" placeholder="العميل" id="filter-customer">
            </div>
            <div class="col">
                <input type="text" class="form-control form-control-sm" placeholder="الاعتماد" id="filter-authorization">
            </div>
            <div class="col">
                <input type="text" class="form-control form-control-sm" placeholder="الترحيل" id="filter-posting">
            </div>
            <div class="col">
                <input type="text" class="form-control form-control-sm" placeholder="المرجع" id="filter-reference">
            </div>
        </div>
        <div class="row g-2 mb-2">
            <div class="col">
                <input type="text" class="form-control form-control-sm" placeholder="المستخدم" id="filter-user">
            </div>
            <div class="col">
                <input type="text" class="form-control form-control-sm" placeholder="المركز المالي" id="filter-financial-center">
            </div>
            <div class="col">
                <input type="text" class="form-control form-control-sm" placeholder="نوع السند" id="filter-voucher-type">
            </div>
            <div class="col">
                <input type="text" class="form-control form-control-sm" placeholder="رقم السند" id="filter-voucher-number">
            </div>
            <div class="col">
                <input type="text" class="form-control form-control-sm" placeholder="2025-09-16" id="filter-date-1">
            </div>
            <div class="col">
                <input type="text" class="form-control form-control-sm" placeholder="2025-09-16" id="filter-date-2">
            </div>
        </div>
        <div class="row g-2">
            <div class="col">
                <button type="button" class="btn btn-secondary btn-sm" id="resetFilters">إعادة تعيين</button>
                <button type="button" class="btn btn-primary btn-sm" id="applyFilters">تطبيق</button>
            </div>
        </div>
    </div>
</div>

<!-- Action buttons -->
<div class="mb-3">
    <div class="d-flex gap-2 flex-wrap">
        <a href="#" class="btn btn-sm btn-outline-danger" title="حذف">
            <i class="ti ti-trash"></i>
        </a>
        <a href="#" class="btn btn-sm btn-outline-success" title="إضافة" data-bs-toggle="modal" data-bs-target="#addVoucherModal">
            <i class="ti ti-plus"></i>
        </a>
        <a href="#" class="btn btn-sm btn-outline-primary" title="تعديل" id="topEditButton">
            <i class="ti ti-pencil"></i>
        </a>
        <a href="#" class="btn btn-sm btn-outline-info" title="مشاهدة" id="topViewButton">
            <i class="ti ti-eye"></i>
        </a>
        <!-- New Popup Menu Button -->
        <a href="#" class="btn btn-sm btn-outline-dark" title="قائمة الإجراءات" data-bs-toggle="modal" data-bs-target="#popupMenuModal">
            <i class="ti ti-menu-2"></i>
        </a>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <div class="d-flex align-items-center">
            <h5 class="card-title mb-0 fw-bold">@ViewData["Title"]</h5>
            <div class="me-auto">
                <!-- Refresh button -->
                <button type="button" class="btn btn-sm btn-outline-secondary ms-2" id="refreshBtn" title="تحديث">
                    <i class="ti ti-refresh"></i>
                </button>
            </div>
        </div>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table id="myTable" class="table table-striped table-bordered dt-responsive nowrap w-100">
                <thead>
                    <tr>
                        <th class="text-center" width="40px">
                            <div class="form-check"><input type="checkbox" class="form-check-input" id="selectAll"></div>
                        </th>
                        <th>id</th>
                        <th>رقم السند</th>
                        <th>تاريخ السند</th>
                        <th>نوع السند</th>
                        <th>الحالة</th>
                        <th>الحساب</th>
                        <th>المركز المالي</th>
                        <th>المبلغ</th>
                        <th>المرجع</th>
                        <th>البيان</th>
                        <th style="width: 100px; text-align: center;">المزيد</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td class="text-center">
                            <div class="form-check"><input type="checkbox" class="form-check-input employee-checkbox" value="1"></div>
                        </td>
                        <td>1</td>
                        <td>VD-001</td>
                        <td>2025-09-14</td>
                        <td>سند خصم</td>
                        <td>مكتمل</td>
                        <td>حساب عميل</td>
                        <td>مركز مالي 1</td>
                        <td>5000.00</td>
                        <td>مرجع 001</td>
                        <td>بيان السند</td>
                        <td style="text-align: center;">
                            <div class="dropdown">
                                <button class="btn btn-sm btn-outline-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    للمزيد
                                </button>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li><a class="dropdown-item" href="#" data-action="edit"><i class="ti ti-pencil me-1"></i> تعديل</a></li>
                                    <li><a class="dropdown-item" href="#" data-action="view"><i class="ti ti-eye me-1"></i> مشاهدة</a></li>
                                    <li><a class="dropdown-item" href="#"><i class="ti ti-trash me-1"></i> حذف</a></li>
                                </ul>
                            </div>
                        </td>
                    </tr>
                    <tr>
                        <td class="text-center">
                            <div class="form-check"><input type="checkbox" class="form-check-input employee-checkbox" value="2"></div>
                        </td>
                        <td>2</td>
                        <td>VD-002</td>
                        <td>2025-09-15</td>
                        <td>سند خصم</td>
                        <td>مكتمل</td>
                        <td>حساب عميل</td>
                        <td>مركز مالي 1</td>
                        <td>3000.00</td>
                        <td>مرجع 002</td>
                        <td>بيان السند</td>
                        <td style="text-align: center;">
                            <div class="dropdown">
                                <button class="btn btn-sm btn-outline-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    للمزيد
                                </button>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li><a class="dropdown-item" href="#" data-action="edit"><i class="ti ti-pencil me-1"></i> تعديل</a></li>
                                    <li><a class="dropdown-item" href="#" data-action="view"><i class="ti ti-eye me-1"></i> مشاهدة</a></li>
                                    <li><a class="dropdown-item" href="#"><i class="ti ti-trash me-1"></i> حذف</a></li>
                                </ul>
                            </div>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- New Popup Menu Modal -->
<div class="modal fade" id="popupMenuModal" tabindex="-1" aria-labelledby="popupMenuModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" style="max-width: 1200px;">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="popupMenuModalLabel">قائمة الإجراءات</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <!-- Popup Menu Content -->
                <div class="container-fluid">
                    <div class="row">
                        <div class="col-md-12 mb-3">
                            <h6 class="text-primary">الإجراءات المتاحة</h6>
                            <p>اختر الإجراء الذي ترغب في تنفيذه من القائمة التالية:</p>
                        </div>
                    </div>
                    
                    <!-- Action Buttons Grid -->
                    <div class="row g-3">
                        <div class="col-md-4">
                            <div class="card h-100">
                                <div class="card-body text-center">
                                    <i class="ti ti-file-plus fs-1 text-primary mb-2"></i>
                                    <h6 class="card-title">إضافة سند</h6>
                                    <p class="card-text">إضافة سند خصم رصيد جديد</p>
                                    <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addVoucherModal">
                                        <i class="ti ti-plus"></i> إضافة
                                    </button>
                                </div>
                            </div>
                        </div>
                        
                        <div class="col-md-4">
                            <div class="card h-100">
                                <div class="card-body text-center">
                                    <i class="ti ti-file-export fs-1 text-success mb-2"></i>
                                    <h6 class="card-title">تصدير البيانات</h6>
                                    <p class="card-text">تصدير بيانات السندات إلى ملف</p>
                                    <button type="button" class="btn btn-success">
                                        <i class="ti ti-download"></i> تصدير
                                    </button>
                                </div>
                            </div>
                        </div>
                        
                        <div class="col-md-4">
                            <div class="card h-100">
                                <div class="card-body text-center">
                                    <i class="ti ti-printer fs-1 text-info mb-2"></i>
                                    <h6 class="card-title">طباعة التقارير</h6>
                                    <p class="card-text">طباعة تقارير السندات</p>
                                    <button type="button" class="btn btn-info">
                                        <i class="ti ti-printer"></i> طباعة
                                    </button>
                                </div>
                            </div>
                        </div>
                        
                        <div class="col-md-4">
                            <div class="card h-100">
                                <div class="card-body text-center">
                                    <i class="ti ti-search fs-1 text-warning mb-2"></i>
                                    <h6 class="card-title">بحث متقدم</h6>
                                    <p class="card-text">إجراء بحث متقدم في بيانات السندات</p>
                                    <button type="button" class="btn btn-warning">
                                        <i class="ti ti-filter"></i> بحث
                                    </button>
                                </div>
                            </div>
                        </div>
                        
                        <div class="col-md-4">
                            <div class="card h-100">
                                <div class="card-body text-center">
                                    <i class="ti ti-settings fs-1 text-secondary mb-2"></i>
                                    <h6 class="card-title">إعدادات النظام</h6>
                                    <p class="card-text">ضبط إعدادات السندات</p>
                                    <button type="button" class="btn btn-secondary">
                                        <i class="ti ti-settings"></i> إعدادات
                                    </button>
                                </div>
                            </div>
                        </div>
                        
                        <div class="col-md-4">
                            <div class="card h-100">
                                <div class="card-body text-center">
                                    <i class="ti ti-help fs-1 text-danger mb-2"></i>
                                    <h6 class="card-title">المساعدة</h6>
                                    <p class="card-text">عرض دليل المستخدم ومساعدة النظام</p>
                                    <button type="button" class="btn btn-danger">
                                        <i class="ti ti-help"></i> مساعدة
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إغلاق</button>
            </div>
        </div>
    </div>
</div>

<!-- ===== نافذة إضافة سند جديد ===== -->
<div class="modal fade voucher-modal" id="addVoucherModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-xl">
    <div class="modal-content" dir="rtl">
      <div class="modal-header">
        <h5 class="modal-title">سند خصم رصيد</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="إغلاق"></button>
      </div>

      <form id="voucherForm">
        <div class="modal-body">

          <!-- (السند) -->
          <div class="v-section">
            <div class="v-cap">(السند)</div>
            <div class="row g-3">
              <div class="col-md-4">
                <label class="v-label">نوع السند <span class="v-req">*</span></label>
                <select class="v-select" id="v-type" required>
                  <option value="">نوع السند</option>
                  <option>سند خصم</option>
                  <option>سند قبض</option>
                  <option>سند صرف</option>
                </select>
              </div>
              <div class="col-md-4">
                <label class="v-label">الرقم <span class="v-req">*</span></label>
                <div class="d-flex gap-2 align-items-center">
                  <!-- حقل المسلسل -->
                  <input id="serial" type="text" class="v-input" placeholder="المسلسل">

                  <!-- حقل الرقم -->
                  <input id="number" type="text" class="v-input" placeholder="الرقم" required>
                </div>
              </div>
              <div class="col-md-4">
                <label class="v-label">التاريخ <span class="v-req">*</span></label>
                <div class="date-field" dir="rtl">
                  <div class="d-wrap">
                    <span class="d-ic" id="openCalendar">📅</span>
                    <input id="v-date" class="d-input" type="text" placeholder="YYYY-MM-DD" required>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- (من حساب) -->
          <div class="v-section">
            <div class="v-cap">(من حساب)</div>
            <div class="row g-3">
              <div class="col-md-6">
                <label class="v-label">المبلغ <span class="v-req">*</span></label>
                <input type="number" step="0.01" class="v-input" id="from-amount" placeholder="المبلغ" required>
              </div>

              <div class="col-md-6">
                <label class="v-label">المركز المالي</label>
                <input class="v-input" id="from-center" placeholder="المركز المالي" disabled>
              </div>

              <div class="col-md-6">
                <label class="v-label">العملة</label>
                <input class="v-input" id="from-currency" placeholder="العملة" disabled>
              </div>

              <div class="col-md-6">
                <label class="v-label">سعر الصرف <span class="v-req">*</span></label>
                <input type="number" step="0.0001" class="v-input" id="from-fx" placeholder="سعر الصرف" required>
              </div>

              <div class="col-md-12">
                <label class="v-label">المقابل بالمحلي</label>
                <input class="v-input" id="from-local">
              </div>

              <!-- (البيان) الفرعي -->
              <div class="col-12">
                <div class="v-section mb-0">
                  <div class="v-cap">(البيان)</div>
                  <div class="row g-3">
                    <div class="col-md-8">
                      <label class="v-label">البيان <span class="v-req">*</span></label>
                      <input class="v-input" id="from-note" value="عليكم سند خصم" required>
                    </div>
                    <div class="col-md-4">
                      <label class="v-label">المرجع</label>
                      <input class="v-input" id="from-ref" placeholder="المرجع">
                    </div>
                  </div>
                </div>
              </div>

            </div>
          </div>

          <!-- (إلى حساب) -->
          <div class="v-section">
            <div class="v-cap">(إلى حساب)</div>
            <div class="row g-3">
              <div class="col-md-6">
                <label class="v-label">الحساب <span class="v-req">*</span></label>
                <select class="v-select" id="to-account" required>
                  <option value="">الحساب</option>
                  <option>الصندوق</option>
                  <option>البنك الرئيسي</option>
                  <option>موردون</option>
                </select>
              </div>

              <div class="col-md-6">
                <label class="v-label">المبلغ <span class="v-req">*</span></label>
                <input type="number" step="0.01" class="v-input" id="to-amount" placeholder="المبلغ" required>
              </div>

              <div class="col-md-6">
                <label class="v-label">المركز المالي</label>
                <input class="v-input" id="to-center" placeholder="المركز المالي" disabled>
              </div>

              <div class="col-md-6">
                <label class="v-label">العملة</label>
                <input class="v-input" id="to-currency" placeholder="العملة" disabled>
              </div>

              <div class="col-md-6">
                <label class="v-label">سعر الصرف <span class="v-req">*</span></label>
                <input type="number" step="0.0001" class="v-input" id="to-fx" placeholder="سعر الصرف" required>
              </div>

              <div class="col-md-12">
                <label class="v-label">المقابل بالمحلي</label>
                <input class="v-input" id="to-local">
              </div>

              <!-- (البيان) الفرعي -->
              <div class="col-12">
                <div class="v-section mb-0">
                  <div class="v-cap">(البيان)</div>
                  <div class="row g-3">
                    <div class="col-md-8">
                      <label class="v-label">البيان <span class="v-req">*</span></label>
                      <input class="v-input" id="to-note" value="لكم سند خصم" required>
                    </div>
                  </div>
                </div>
              </div>

            </div>
          </div>

        </div>

        <div class="modal-footer">
          <small class="text-muted me-auto">الحقول بعلامة <span class="v-req">*</span> مطلوبة</small>
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إلغاء</button>
          <button type="submit" class="btn btn-primary">حفظ السند</button>
        </div>
      </form>
    </div>
  </div>
</div>

<!-- ===== نافذة تعديل السند ===== -->
<div class="modal fade voucher-modal" id="editVoucherModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-xl">
    <div class="modal-content" dir="rtl">
      <div class="modal-header">
        <h5 class="modal-title">تعديل سند خصم رصيد</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="إغلاق"></button>
      </div>

      <form id="editVoucherForm">
        <div class="modal-body">

          <!-- (السند) -->
          <div class="v-section">
            <div class="v-cap">(السند)</div>
            <div class="row g-3">
              <div class="col-md-4">
                <label class="v-label">نوع السند <span class="v-req">*</span></label>
                <select class="v-select" id="edit-v-type" required>
                  <option value="">نوع السند</option>
                  <option>سند خصم</option>
                  <option>سند قبض</option>
                  <option>سند صرف</option>
                </select>
              </div>
              <div class="col-md-4">
                <label class="v-label">الرقم <span class="v-req">*</span></label>
                <div class="d-flex gap-2 align-items-center">
                  <!-- حقل المسلسل -->
                  <input id="edit-serial" type="text" class="v-input" placeholder="المسلسل">

                  <!-- حقل الرقم -->
                  <input id="edit-number" type="text" class="v-input" placeholder="الرقم" required>
                </div>
              </div>
              <div class="col-md-4">
                <label class="v-label">التاريخ <span class="v-req">*</span></label>
                <div class="date-field" dir="rtl">
                  <div class="d-wrap">
                    <span class="d-ic" id="openEditCalendar">📅</span>
                    <input id="edit-v-date" class="d-input" type="text" placeholder="YYYY-MM-DD" required>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- (من حساب) -->
          <div class="v-section">
            <div class="v-cap">(من حساب)</div>
            <div class="row g-3">
              <div class="col-md-6">
                <label class="v-label">المبلغ <span class="v-req">*</span></label>
                <input type="number" step="0.01" class="v-input" id="edit-from-amount" placeholder="المبلغ" required>
              </div>

              <div class="col-md-6">
                <label class="v-label">المركز المالي</label>
                <input class="v-input" id="edit-from-center" placeholder="المركز المالي" disabled>
              </div>

              <div class="col-md-6">
                <label class="v-label">العملة</label>
                <input class="v-input" id="edit-from-currency" placeholder="العملة" disabled>
              </div>

              <div class="col-md-6">
                <label class="v-label">سعر الصرف <span class="v-req">*</span></label>
                <input type="number" step="0.0001" class="v-input" id="edit-from-fx" placeholder="سعر الصرف" required>
              </div>

              <div class="col-md-12">
                <label class="v-label">المقابل بالمحلي</label>
                <input class="v-input" id="edit-from-local">
              </div>

              <!-- (البيان) الفرعي -->
              <div class="col-12">
                <div class="v-section mb-0">
                  <div class="v-cap">(البيان)</div>
                  <div class="row g-3">
                    <div class="col-md-8">
                      <label class="v-label">البيان <span class="v-req">*</span></label>
                      <input class="v-input" id="edit-from-note" value="عليكم سند خصم" required>
                    </div>
                    <div class="col-md-4">
                      <label class="v-label">المرجع</label>
                      <input class="v-input" id="edit-from-ref" placeholder="المرجع">
                    </div>
                  </div>
                </div>
              </div>

            </div>
          </div>

          <!-- (إلى حساب) -->
          <div class="v-section">
            <div class="v-cap">(إلى حساب)</div>
            <div class="row g-3">
              <div class="col-md-6">
                <label class="v-label">الحساب <span class="v-req">*</span></label>
                <select class="v-select" id="edit-to-account" required>
                  <option value="">الحساب</option>
                  <option>الصندوق</option>
                  <option>البنك الرئيسي</option>
                  <option>موردون</option>
                </select>
              </div>

              <div class="col-md-6">
                <label class="v-label">المبلغ <span class="v-req">*</span></label>
                <input type="number" step="0.01" class="v-input" id="edit-to-amount" placeholder="المبلغ" required>
              </div>

              <div class="col-md-6">
                <label class="v-label">المركز المالي</label>
                <input class="v-input" id="edit-to-center" placeholder="المركز المالي" disabled>
              </div>

              <div class="col-md-6">
                <label class="v-label">العملة</label>
                <input class="v-input" id="edit-to-currency" placeholder="العملة" disabled>
              </div>

              <div class="col-md-6">
                <label class="v-label">سعر الصرف <span class="v-req">*</span></label>
                <input type="number" step="0.0001" class="v-input" id="edit-to-fx" placeholder="سعر الصرف" required>
              </div>

              <div class="col-md-12">
                <label class="v-label">المقابل بالمحلي</label>
                <input class="v-input" id="edit-to-local">
              </div>

              <!-- (البيان) الفرعي -->
              <div class="col-12">
                <div class="v-section mb-0">
                  <div class="v-cap">(البيان)</div>
                  <div class="row g-3">
                    <div class="col-md-8">
                      <label class="v-label">البيان <span class="v-req">*</span></label>
                      <input class="v-input" id="edit-to-note" value="لكم سند خصم" required>
                    </div>
                  </div>
                </div>
              </div>

            </div>
          </div>

        </div>

        <div class="modal-footer">
          <small class="text-muted me-auto">الحقول بعلامة <span class="v-req">*</span> مطلوبة</small>
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إلغاء</button>
          <button type="submit" class="btn btn-primary">حفظ التعديلات</button>
        </div>
      </form>
    </div>
  </div>
</div>

<!-- ===== نافذة مشاهدة السند ===== -->
<div class="modal fade voucher-modal" id="viewVoucherModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-xl">
    <div class="modal-content" dir="rtl">
      <div class="modal-header">
        <h5 class="modal-title">عرض سند خصم رصيد</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="إغلاق"></button>
      </div>

      <div class="modal-body">

        <!-- (السند) -->
        <div class="v-section">
          <div class="v-cap">(السند)</div>
          <div class="row g-3">
            <div class="col-md-4">
              <label class="v-label">نوع السند</label>
              <div class="view-field" id="view-v-type"></div>
            </div>
            <div class="col-md-4">
              <label class="v-label">الرقم</label>
              <div class="d-flex gap-2 align-items-center">
                <!-- حقل المسلسل -->
                <div class="view-field" id="view-v-serial"></div>
                <!-- حقل الرقم -->
                <div class="view-field" id="view-v-number"></div>
              </div>
            </div>
            <div class="col-md-4">
              <label class="v-label">التاريخ</label>
              <div class="view-field" id="view-v-date"></div>
            </div>
          </div>
        </div>

        <!-- (من حساب) -->
        <div class="v-section">
          <div class="v-cap">(من حساب)</div>
          <div class="row g-3">
            <div class="col-md-6">
              <label class="v-label">المبلغ</label>
              <div class="view-field" id="view-from-amount"></div>
            </div>

            <div class="col-md-6">
              <label class="v-label">المركز المالي</label>
              <div class="view-field" id="view-from-center"></div>
            </div>

            <div class="col-md-6">
              <label class="v-label">العملة</label>
              <div class="view-field" id="view-from-currency"></div>
            </div>

            <div class="col-md-6">
              <label class="v-label">سعر الصرف</label>
              <div class="view-field" id="view-from-fx"></div>
            </div>

            <div class="col-md-12">
              <label class="v-label">المقابل بالمحلي</label>
              <div class="view-field" id="view-from-local"></div>
            </div>

            <!-- (البيان) الفرعي -->
            <div class="col-12">
              <div class="v-section mb-0">
                <div class="v-cap">(البيان)</div>
                <div class="row g-3">
                  <div class="col-md-8">
                    <label class="v-label">البيان</label>
                    <div class="view-field" id="view-from-note"></div>
                  </div>
                  <div class="col-md-4">
                    <label class="v-label">المرجع</label>
                    <div class="view-field" id="view-from-ref"></div>
                  </div>
                </div>
              </div>
            </div>

          </div>
        </div>

        <!-- (إلى حساب) -->
        <div class="v-section">
          <div class="v-cap">(إلى حساب)</div>
          <div class="row g-3">
            <div class="col-md-6">
              <label class="v-label">الحساب</label>
              <div class="view-field" id="view-to-account"></div>
            </div>

            <div class="col-md-6">
              <label class="v-label">المبلغ</label>
              <div class="view-field" id="view-to-amount"></div>
            </div>

            <div class="col-md-6">
              <label class="v-label">المركز المالي</label>
              <div class="view-field" id="view-to-center"></div>
            </div>

            <div class="col-md-6">
              <label class="v-label">العملة</label>
              <div class="view-field" id="view-to-currency"></div>
            </div>

            <div class="col-md-6">
              <label class="v-label">سعر الصرف</label>
              <div class="view-field" id="view-to-fx"></div>
            </div>

            <div class="col-md-12">
              <label class="v-label">المقابل بالمحلي</label>
              <div class="view-field" id="view-to-local"></div>
            </div>

            <!-- (البيان) الفرعي -->
            <div class="col-12">
              <div class="v-section mb-0">
                <div class="v-cap">(البيان)</div>
                <div class="row g-3">
                  <div class="col-md-8">
                    <label class="v-label">البيان</label>
                    <div class="view-field" id="view-to-note"></div>
                  </div>
                </div>
              </div>
            </div>

          </div>
        </div>

      </div>

      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إغلاق</button>
        <button type="button" class="btn btn-primary" id="editFromViewBtn">تعديل</button>
      </div>
    </div>
  </div>
</div>

@section Scripts {
<!-- jstree plugin Js-->
<script src="~/plugins/jstree/jstree.min.js"></script>
<script src="~/js/account-tree.js"></script>
<!-- flatpickr Js -->
<script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
<script src="https://cdn.jsdelivr.net/npm/flatpickr/dist/l10n/ar.js"></script>

<script>
  $(function () {
    // Show debug message
    $('#debug-message').show();
    
    // Test jQuery
    if (typeof $ === 'undefined') {
        $('#debug-message').removeClass('alert-info').addClass('alert-danger')
            .text('jQuery لم يتم تحميله بشكل صحيح');
        return;
    } else {
        $('#debug-message').append('<br>jQuery تم تحميله بشكل صحيح');
    }
    
    // Initialize the tree view
    try {
        if (typeof $.fn.jstree === 'undefined') {
            $('#debug-message').removeClass('alert-info').addClass('alert-danger')
                .text('jstree plugin لم يتم تحميله بشكل صحيح');
            return;
        }
        
        $('#trusted-customers-tree').jstree({
            core: {
                themes: {responsive: false},
                data: @Html.Raw(Json.Serialize(Model.TreeData))
            },
            plugins: ["types", "contextmenu"],
            types: {
                default: {icon: "ti ti-folder-filled fs-lg text-warning"},
                file: {icon: "ti ti-file-text fs-lg text-primary"}
            },
            contextmenu: {
                items: function(node) {
                    return {
                        "edit": {
                            "label": "تعديل",
                            "icon": "ti ti-pencil",
                            "action": function (obj) {
                                editCustomer(node);
                            }
                        },
                        "view": {
                            "label": "مشاهدة",
                            "icon": "ti ti-eye",
                            "action": function (obj) {
                                viewCustomer(node);
                            }
                        },
                        "delete": {
                            "label": "حذف",
                            "icon": "ti ti-trash",
                            "action": function (obj) {
                                deleteCustomer(node);
                            }
                        }
                    };
                }
            }
        }).on('ready.jstree', function() {
            $('#debug-message').append('<br>jstree تم تهيئة الشجرة بشكل صحيح');
        }).on('error.jstree', function(e, data) {
            console.error('jstree error:', data);
            $('#debug-message').removeClass('alert-info').addClass('alert-danger')
                .text('حدث خطأ أثناء تهيئة jstree: ' + data.reason);
        });
    } catch (error) {
        console.error('Error initializing jstree:', error);
        $('#debug-message').removeClass('alert-info').addClass('alert-danger')
            .text('حدث خطأ أثناء تحميل شجرة العملاء: ' + error.message);
    }
    
    // Handle popup menu actions
    $('#popupMenuModal').on('click', '.btn', function() {
      var buttonText = $(this).text().trim();
      
      // Handle export action
      if (buttonText.includes('تصدير')) {
        alert('سيتم تصدير بيانات السندات إلى ملف');
      }
      // Handle print action
      else if (buttonText.includes('طباعة')) {
        alert('سيتم طباعة تقارير السندات');
      }
      // Handle search action
      else if (buttonText.includes('بحث')) {
        alert('سيتم فتح نافذة البحث المتقدم');
      }
      // Handle settings action
      else if (buttonText.includes('إعدادات')) {
        alert('سيتم فتح إعدادات السندات');
      }
      // Handle help action
      else if (buttonText.includes('مساعدة')) {
        alert('سيتم عرض دليل المستخدم ومساعدة النظام');
      }
    });
    
    // When opening the popup menu, close any other open modals
    $('[data-bs-target="#popupMenuModal"]').on('click', function() {
      $('.modal').modal('hide');
    });
    
    // Handle view action from dropdown menu
    $('tbody').on('click', '[data-action="view"]', function(e) {
      e.preventDefault();
      
      // Get the voucher data from the row
      var row = $(this).closest('tr');
      var voucherId = row.find('td:nth-child(2)').text();
      var voucherNumber = row.find('td:nth-child(3)').text();
      var voucherDate = row.find('td:nth-child(4)').text();
      var voucherType = row.find('td:nth-child(5)').text();
      var status = row.find('td:nth-child(6)').text();
      var account = row.find('td:nth-child(7)').text();
      var financialCenter = row.find('td:nth-child(8)').text();
      var amount = row.find('td:nth-child(9)').text();
      var reference = row.find('td:nth-child(10)').text();
      var description = row.find('td:nth-child(11)').text();
      
      // Populate the view modal with voucher data
      $('#view-v-type').text(voucherType);
      // Split voucherNumber into serial and number parts
      var serial = '';
      var number = voucherNumber;
      // Split based on the format VD-001 where VD is serial and 001 is number
      if (voucherNumber && voucherNumber.includes('-')) {
        var parts = voucherNumber.split('-');
        serial = parts[0];
        number = parts[1] || '';
      } else if (voucherNumber) {
        // If no dash, assume the whole thing is the number
        number = voucherNumber;
      }
      $('#view-v-serial').text(serial);
      $('#view-v-number').text(number);
      $('#view-v-date').text(voucherDate);
      $('#view-from-amount').text(amount);
      $('#view-from-center').text(financialCenter);
      $('#view-from-currency').text('USD'); // Sample data
      $('#view-from-fx').text('1.0000'); // Sample data
      $('#view-from-local').text(amount); // Sample data
      $('#view-from-note').text(description);
      $('#view-from-ref').text(reference);
      $('#view-to-account').text(account);
      $('#view-to-amount').text(amount);
      $('#view-to-center').text(financialCenter);
      $('#view-to-currency').text('USD'); // Sample data
      $('#view-to-fx').text('1.0000'); // Sample data
      $('#view-to-local').text(amount); // Sample data
      $('#view-to-note').text(description);
      
      // Show the view modal
      $('#viewVoucherModal').modal('show');
    });
    
    // Handle edit action from dropdown menu
    $('tbody').on('click', '[data-action="edit"]', function(e) {
      e.preventDefault();
      
      // Get the voucher data from the row
      var row = $(this).closest('tr');
      var voucherId = row.find('td:nth-child(2)').text();
      var voucherNumber = row.find('td:nth-child(3)').text();
      var voucherDate = row.find('td:nth-child(4)').text();
      var voucherType = row.find('td:nth-child(5)').text();
      var status = row.find('td:nth-child(6)').text();
      var account = row.find('td:nth-child(7)').text();
      var financialCenter = row.find('td:nth-child(8)').text();
      var amount = row.find('td:nth-child(9)').text();
      var reference = row.find('td:nth-child(10)').text();
      var description = row.find('td:nth-child(11)').text();
      
      // Populate the edit modal with voucher data
      $('#edit-v-type').val(voucherType);
      // Split voucherNumber into serial and number parts
      var serial = '';
      var number = voucherNumber;
      // Split based on the format VD-001 where VD is serial and 001 is number
      if (voucherNumber && voucherNumber.includes('-')) {
        var parts = voucherNumber.split('-');
        serial = parts[0];
        number = parts[1] || '';
      } else if (voucherNumber) {
        // If no dash, assume the whole thing is the number
        number = voucherNumber;
      }
      $('#edit-serial').val(serial);
      $('#edit-number').val(number);
      $('#edit-v-date').val(voucherDate);
      $('#edit-from-amount').val(amount);
      $('#edit-from-center').val(financialCenter);
      $('#edit-from-currency').val('USD'); // Sample data
      $('#edit-from-fx').val('1.0000'); // Sample data
      $('#edit-from-local').val(amount); // Sample data
      $('#edit-from-note').val(description);
      $('#edit-from-ref').val(reference);
      $('#edit-to-account').val(account);
      $('#edit-to-amount').val(amount);
      $('#edit-to-center').val(financialCenter);
      $('#edit-to-currency').val('USD'); // Sample data
      $('#edit-to-fx').val('1.0000'); // Sample data
      $('#edit-to-local').val(amount); // Sample data
      $('#edit-to-note').val(description);
      
      // Show the edit modal
      $('#editVoucherModal').modal('show');
    });
    
    // Handle select all checkbox
    $('#selectAll').on('change', function() {
      $('.employee-checkbox').prop('checked', $(this).is(':checked'));
    });
    
    // Handle individual checkboxes
    $('tbody').on('change', '.employee-checkbox', function() {
      $('#selectAll').prop('checked', $('.employee-checkbox').length === $('.employee-checkbox:checked').length);
    });
    
    // Initialize DataTable
    const dt = $("#myTable").DataTable({
      paging: true, lengthChange: false, searching: false, ordering: true, info: true, autoWidth: false, responsive: true,
      language: {
        emptyTable: "لا توجد بيانات",
        info: "إظهار _START_ إلى _END_ من _TOTAL_ سجل",
        infoEmpty: "إظهار 0 إلى 0 من 0",
        paginate: { first:"الأولى", last:"الأخيرة", next:"التالي", previous:"السابق" }
      }
    });
    
    // ===== Flatpickr for new voucher modal =====
    $("#voucherDate").flatpickr({
      dateFormat: "Y-m-d",
      locale: "ar"
    });
    
    // ===== Context Menu =====
    $.contextMenu({
      selector: '.tree-node',
      callback: function(key, options) {
        var node = options.$trigger.data('node');
        switch (key) {
          case 'view':
            viewCustomer(node);
            break;
          case 'edit':
            editCustomer(node);
            break;
          case 'delete':
            deleteCustomer(node);
            break;
        }
      },
      items: {
        view: { name: "عرض", icon: "fa-eye" },
        edit: { name: "تعديل", icon: "fa-edit" },
        delete: { name: "حذف", icon: "fa-trash" }
      }
    });
    
    // Handle view action from context menu
    function viewCustomer(node) {
      // Check if a node is selected
      if (!node) {
        alert('الرجاء تحديد حساب من الشجرة لعرضه');
        return;
      }
      
      // Populate the view modal with actual node data
      $('#viewVoucherNumber').text(node.id);
      $('#viewVoucherDate').text(new Date().toISOString().split('T')[0]);
      $('#viewVoucherType').text(node.text);
      $('#viewVoucherStatus').text('مكتمل');
      $('#viewAccount').text(node.text);
      $('#viewFinancialCenter').text('مركز مالي 1');
      $('#viewAmount').text('0.00');
      $('#viewReference').text('');
      $('#viewDescription').text('');
      
      // Show the view modal
      $('#viewCustomerModal').modal('show');
    }
    
    // Handle edit action from context menu
    function editCustomer(node) {
      // Check if a node is selected
      if (!node) {
        alert('الرجاء تحديد حساب من الشجرة لتعديله');
        return;
      }
      
      // Populate the edit modal with actual node data
      $('#editVoucherNumber').val(node.id);
      $('#editVoucherDate').val(new Date().toISOString().split('T')[0]);
      $('#editVoucherType').val(node.text);
      $('#editVoucherStatus').val('مكتمل');
      $('#editAccount').val(node.text);
      $('#editFinancialCenter').val('مركز مالي 1');
      $('#editAmount').val('0.00');
      $('#editReference').val('');
      $('#editDescription').val('');
      
      // Show the edit modal
      $('#editCustomerModal').modal('show');
    }
    
    // Handle delete action from context menu
    function deleteCustomer(node) {
      // Check if a node is selected
      if (!node) {
        alert('الرجاء تحديد حساب من الشجرة لحذفه');
        return;
      }
      
      if (confirm('هل أنت متأكد من حذف هذا السند؟')) {
        // In a real application, you would send a request to the server to delete the voucher
        alert('تم حذف السند بنجاح!');
      }
    }
    
    // Handle the top view button click
    $('#topViewButton').on('click', function(e) {
      e.preventDefault();
      var selectedRows = $('.employee-checkbox:checked');
      if (selectedRows.length === 0) {
        alert('الرجاء تحديد سند من الجدول لعرضه');
      } else if (selectedRows.length === 1) {
        // Get data from the selected row
        var row = selectedRows.closest('tr');
        var voucherId = row.find('td:nth-child(2)').text();
        var voucherNumber = row.find('td:nth-child(3)').text();
        var voucherDate = row.find('td:nth-child(4)').text();
        var voucherType = row.find('td:nth-child(5)').text();
        var status = row.find('td:nth-child(6)').text();
        var account = row.find('td:nth-child(7)').text();
        var financialCenter = row.find('td:nth-child(8)').text();
        var amount = row.find('td:nth-child(9)').text();
        var reference = row.find('td:nth-child(10)').text();
        var description = row.find('td:nth-child(11)').text();
        
        // Split voucherNumber into serial and number parts
        var serial = '';
        var number = voucherNumber;
        // Split based on the format VD-001 where VD is serial and 001 is number
        if (voucherNumber && voucherNumber.includes('-')) {
          var parts = voucherNumber.split('-');
          serial = parts[0];
          number = parts[1] || '';
        } else if (voucherNumber) {
          // If no dash, assume the whole thing is the number
          number = voucherNumber;
        }
        
        // Populate the view modal with voucher data (using the new voucher modals)
        $('#view-v-type').text(voucherType);
        $('#view-v-serial').text(serial);
        $('#view-v-number').text(number);
        $('#view-v-date').text(voucherDate);
        $('#view-from-amount').text(amount);
        $('#view-from-center').text(financialCenter);
        $('#view-from-currency').text('USD'); // Sample data
        $('#view-from-fx').text('1.0000'); // Sample data
        $('#view-from-local').text(amount); // Sample data
        $('#view-from-note').text(description);
        $('#view-from-ref').text(reference);
        $('#view-to-account').text(account);
        $('#view-to-amount').text(amount);
        $('#view-to-center').text(financialCenter);
        $('#view-to-currency').text('USD'); // Sample data
        $('#view-to-fx').text('1.0000'); // Sample data
        $('#view-to-local').text(amount); // Sample data
        $('#view-to-note').text(description);
        
        // Show the view modal
        $('#viewVoucherModal').modal('show');
      } else {
        alert('الرجاء تحديد سند واحد فقط لعرضه');
      }
    });
    
    // Handle the top edit button click
    $('#topEditButton').on('click', function(e) {
      e.preventDefault();
      var selectedRows = $('.employee-checkbox:checked');
      if (selectedRows.length === 0) {
        alert('الرجاء تحديد سند من الجدول لتعديله');
      } else if (selectedRows.length === 1) {
        // Get data from the selected row
        var row = selectedRows.closest('tr');
        var voucherId = row.find('td:nth-child(2)').text();
        var voucherNumber = row.find('td:nth-child(3)').text();
        var voucherDate = row.find('td:nth-child(4)').text();
        var voucherType = row.find('td:nth-child(5)').text();
        var status = row.find('td:nth-child(6)').text();
        var account = row.find('td:nth-child(7)').text();
        var financialCenter = row.find('td:nth-child(8)').text();
        var amount = row.find('td:nth-child(9)').text();
        var reference = row.find('td:nth-child(10)').text();
        var description = row.find('td:nth-child(11)').text();
        
        // Split voucherNumber into serial and number parts
        var serial = '';
        var number = voucherNumber;
        // Split based on the format VD-001 where VD is serial and 001 is number
        if (voucherNumber && voucherNumber.includes('-')) {
          var parts = voucherNumber.split('-');
          serial = parts[0];
          number = parts[1] || '';
        } else if (voucherNumber) {
          // If no dash, assume the whole thing is the number
          number = voucherNumber;
        }
        
        // Populate the edit modal with voucher data (using the new voucher modals)
        $('#edit-v-type').val(voucherType);
        $('#edit-serial').val(serial);
        $('#edit-number').val(number);
        $('#edit-v-date').val(voucherDate);
        $('#edit-from-amount').val(amount);
        $('#edit-from-center').val(financialCenter);
        $('#edit-from-currency').val('USD'); // Sample data
        $('#edit-from-fx').val('1.0000'); // Sample data
        $('#edit-from-local').val(amount); // Sample data
        $('#edit-from-note').val(description);
        $('#edit-from-ref').val(reference);
        $('#edit-to-account').val(account);
        $('#edit-to-amount').val(amount);
        $('#edit-to-center').val(financialCenter);
        $('#edit-to-currency').val('USD'); // Sample data
        $('#edit-to-fx').val('1.0000'); // Sample data
        $('#edit-to-local').val(amount); // Sample data
        $('#edit-to-note').val(description);
        
        // Show the edit modal
        $('#editVoucherModal').modal('show');
      } else {
        alert('الرجاء تحديد سند واحد فقط لتعديله');
      }
    });
    
    // Handle edit button in view modal
    $('#editFromViewBtn').on('click', function() {
      // Hide view modal and show edit modal
      $('#viewVoucherModal').modal('hide');
      $('#editVoucherModal').modal('show');
    });
    
    // Handle filter toggle button
    $('#toggleFilters').on('click', function() {
      var filterSection = $('#filterSection');
      var icon = $(this).find('i');
      var textSpan = $('#toggleFiltersText');
      
      if (filterSection.is(':visible')) {
        // Hide the filter section
        filterSection.hide();
        icon.removeClass('ti-filter-off').addClass('ti-filter');
        textSpan.text('إظهار الفلترة');
      } else {
        // Show the filter section
        filterSection.show();
        icon.removeClass('ti-filter').addClass('ti-filter-off');
        textSpan.text('إخفاء الفلترة');
      }
    });
    
    // Handle apply filters button
    $('#applyFilters').on('click', function() {
      // Get filter values
      var filterValue = $('#filter-value').val();
      var filterCustomer = $('#filter-customer').val();
      var filterAuthorization = $('#filter-authorization').val();
      var filterPosting = $('#filter-posting').val();
      var filterReference = $('#filter-reference').val();
      var filterUser = $('#filter-user').val();
      var filterFinancialCenter = $('#filter-financial-center').val();
      var filterVoucherType = $('#filter-voucher-type').val();
      var filterVoucherNumber = $('#filter-voucher-number').val();
      var filterDate1 = $('#filter-date-1').val();
      var filterDate2 = $('#filter-date-2').val();
      
      // In a real application, you would apply the filters here
      alert('سيتم تطبيق الفلاتر المحددة:\n' +
            'القيمة: ' + filterValue + '\n' +
            'العميل: ' + filterCustomer + '\n' +
            'الاعتماد: ' + filterAuthorization + '\n' +
            'الترحيل: ' + filterPosting + '\n' +
            'المرجع: ' + filterReference + '\n' +
            'المستخدم: ' + filterUser + '\n' +
            'المركز المالي: ' + filterFinancialCenter + '\n' +
            'نوع السند: ' + filterVoucherType + '\n' +
            'رقم السند: ' + filterVoucherNumber + '\n' +
            'التاريخ 1: ' + filterDate1 + '\n' +
            'التاريخ 2: ' + filterDate2);
    });
    
    // Handle reset filters button
    $('#resetFilters').on('click', function() {
      // Reset all filter input fields
      $('#filter-value').val('');
      $('#filter-customer').val('');
      $('#filter-authorization').val('');
      $('#filter-posting').val('');
      $('#filter-reference').val('');
      $('#filter-user').val('');
      $('#filter-financial-center').val('');
      $('#filter-voucher-type').val('');
      $('#filter-voucher-number').val('');
      $('#filter-date-1').val('');
      $('#filter-date-2').val('');
      // In a real application, you would also reset the tree data
      alert('تم إعادة تعيين الفلاتر');
    });
    
    // Handle refresh button
    $('#refreshBtn').on('click', function() {
      // In a real application, you would refresh the table data
      alert('سيتم تحديث بيانات الجدول');
    });
  
    // Datatables js
    // Remove the incorrect script tags and duplicate DataTables initialization
    // The DataTables is already initialized with the table

  });
  
  // Update amount displays when amounts change
  $('#from-amount, #to-amount, #edit-from-amount, #edit-to-amount').on('input', function() {
    // Removed non-existent amount display elements
  });
  
  // Handle save button click with enhanced validation for add voucher form
  $('#voucherForm').on('submit', function(e) {
    e.preventDefault();
      // Reset validation states
      $('#voucherForm .is-invalid').removeClass('is-invalid');
      
      // Validation
      var isValid = true;
      var requiredFields = [
        '#v-type', '#number',
        '#from-amount', '#from-fx',
        '#to-account', '#to-amount', '#to-fx',
        '#from-note', '#to-note'
      ];
      
      requiredFields.forEach(function(field) {
        if (!$(field).val()) {
          $(field).addClass('is-invalid');
          isValid = false;
        }
      });
      
      // Special validation for date picker
      var voucherDate = $('#v-date').val();
      if (!voucherDate) {
          $('#v-date').addClass('is-invalid');
          isValid = false;
      }
      
      // Additional validation for amounts
      var fromAmount = parseFloat($('#from-amount').val());
      var toAmount = parseFloat($('#to-amount').val());
      
      if (isNaN(fromAmount) || fromAmount <= 0) {
        $('#from-amount').addClass('is-invalid');
        isValid = false;
      }
      
      if (isNaN(toAmount) || toAmount <= 0) {
        $('#to-amount').addClass('is-invalid');
        isValid = false;
      }
      
      // Additional validation for exchange rates
      var fromFx = parseFloat($('#from-fx').val());
      var toFx = parseFloat($('#to-fx').val());
      
      if (isNaN(fromFx) || fromFx <= 0) {
        $('#from-fx').addClass('is-invalid');
        isValid = false;
      }
      
      if (isNaN(toFx) || toFx <= 0) {
        $('#to-fx').addClass('is-invalid');
        isValid = false;
      }
      
      // Date format validation (must be YYYY-MM-DD)
      if (voucherDate) {
          var dateRegex = /^\d{4}-\d{2}-\d{2}$/;
          if (!dateRegex.test(voucherDate)) {
              $('#v-date').addClass('is-invalid');
              isValid = false;
          }
      }
      
      if (isValid) {
        // In a real application, you would send the data to the server here
        alert('تم حفظ السند بنجاح (نموذج توضيحي)\nراجع وحدة التحكم للبيانات.');
        $('#addVoucherModal').modal('hide');
        // Reset form
        $('#voucherForm')[0].reset();
        // Reset date pickers
        if (typeof flatpickr !== 'undefined') {
            var today = new Date().toISOString().split('T')[0];
            var voucherDateInstance = document.querySelector("#v-date")._flatpickr;
            if (voucherDateInstance) {
              voucherDateInstance.setDate(today);
            }
        }
      } else {
        alert('الرجاء ملء جميع الحقول المطلوبة بشكل صحيح');
      }
    });
    
    // Handle save button click with enhanced validation for edit voucher form
    $('#editVoucherForm').on('submit', function(e) {
      e.preventDefault();
      // Reset validation states
      $('#editVoucherForm .is-invalid').removeClass('is-invalid');
      
      // Validation
      var isValid = true;
      var requiredFields = [
        '#edit-v-type', '#edit-number',
        '#edit-from-amount', '#edit-from-fx',
        '#edit-to-account', '#edit-to-amount', '#edit-to-fx',
        '#edit-from-note', '#edit-to-note'
      ];
      
      requiredFields.forEach(function(field) {
        if (!$(field).val()) {
          $(field).addClass('is-invalid');
          isValid = false;
        }
      });
      
      // Special validation for date picker
      var voucherDate = $('#edit-v-date').val();
      if (!voucherDate) {
          $('#edit-v-date').addClass('is-invalid');
          isValid = false;
      }
      
      // Additional validation for amounts
      var fromAmount = parseFloat($('#edit-from-amount').val());
      var toAmount = parseFloat($('#edit-to-amount').val());
      
      if (isNaN(fromAmount) || fromAmount <= 0) {
        $('#edit-from-amount').addClass('is-invalid');
        isValid = false;
      }
      
      if (isNaN(toAmount) || toAmount <= 0) {
        $('#edit-to-amount').addClass('is-invalid');
        isValid = false;
      }
      
      // Additional validation for exchange rates
      var fromFx = parseFloat($('#edit-from-fx').val());
      var toFx = parseFloat($('#edit-to-fx').val());
      
      if (isNaN(fromFx) || fromFx <= 0) {
        $('#edit-from-fx').addClass('is-invalid');
        isValid = false;
      }
      
      if (isNaN(toFx) || toFx <= 0) {
        $('#edit-to-fx').addClass('is-invalid');
        isValid = false;
      }
      
      // Date format validation (must be YYYY-MM-DD)
      if (voucherDate) {
          var dateRegex = /^\d{4}-\d{2}-\d{2}$/;
          if (!dateRegex.test(voucherDate)) {
              $('#edit-v-date').addClass('is-invalid');
              isValid = false;
          }
      }
      
      if (isValid) {
        // In a real application, you would send the data to the server here
        alert('تم حفظ التعديلات بنجاح (نموذج توضيحي)\nراجع وحدة التحكم للبيانات.');
        $('#editVoucherModal').modal('hide');
      } else {
        alert('الرجاء ملء جميع الحقول المطلوبة بشكل صحيح');
      }
    });
    
    // Open modal when clicking the "جديد" button in popup menu
    $('#popupMenuModal').on('click', '.btn-primary', function() {
      $('#addVoucherModal').modal('show');
    });
    
    // Add validation classes on focusout for required fields
    $('#voucherForm input[required], #voucherForm select[required], #editVoucherForm input[required], #editVoucherForm select[required]').on('focusout', function() {
      if (!$(this).val()) {
        $(this).addClass('is-invalid');
      } else {
        $(this).removeClass('is-invalid');
      }
    });
    
    // Remove validation error when user starts typing
    $('#voucherForm input, #voucherForm select, #editVoucherForm input, #editVoucherForm select').on('input change', function() {
      $(this).removeClass('is-invalid');
    });
    
    // Handle select all checkbox
    $('#selectAll').on('change', function() {
      var isChecked = $(this).is(':checked');
      $('.employee-checkbox').prop('checked', isChecked);
    });
    
    // Handle individual checkboxes
    $('tbody').on('change', '.employee-checkbox', function() {
      var allChecked = $('.employee-checkbox').length === $('.employee-checkbox:checked').length;
      $('#selectAll').prop('checked', allChecked);
    });
    
    // ===== Flatpickr for new voucher modal =====
    (function () {
      const modal = document.getElementById('addVoucherModal');
      let picker = null;

      modal.addEventListener('shown.bs.modal', function () {
        // Always reinitialize to ensure it works with dynamic content
        if (picker) {
          picker.destroy();
        }
        picker = flatpickr('#v-date', {
          dateFormat: 'Y-m-d',
          allowInput: true,
          clickOpens: true,
          appendTo: modal,                      // يضمن الظهور فوق المودال
          position: 'auto',
          locale: Object.assign({}, flatpickr.l10ns.ar, { firstDayOfWeek: 6 }),
          defaultDate: new Date(),
          onReady: function(_, __, inst){
            inst.calendarContainer.style.zIndex = 200000;
            inst.calendarContainer.dir = 'rtl';
          }
        });
        // Ensure the calendar icon opens the date picker
        const openCalendarBtn = document.getElementById('openCalendar');
        if (openCalendarBtn) {
          openCalendarBtn.addEventListener('click', () => {
            if (picker) picker.open();
          });
        }
      });

      // مثال تحقق بسيط عند الحفظ
      document.getElementById('voucherForm').addEventListener('submit', function (e) {
        e.preventDefault();
        if (!this.checkValidity()) return this.reportValidity();
        alert('تم حفظ السند (نموذج توضيحي)');
        bootstrap.Modal.getInstance(modal).hide();
        this.reset();
        if (picker) picker.clear();
      });
    })();

    // ===== Flatpickr for edit voucher modal =====
    (function () {
      const modal = document.getElementById('editVoucherModal');
      let picker = null;

      modal.addEventListener('shown.bs.modal', function () {
        // Always reinitialize to ensure it works with dynamic content
        if (picker) {
          picker.destroy();
        }
        picker = flatpickr('#edit-v-date', {
          dateFormat: 'Y-m-d',
          allowInput: true,
          clickOpens: true,
          appendTo: modal,                      // يضمن الظهور فوق المودال
          position: 'auto',
          locale: Object.assign({}, flatpickr.l10ns.ar, { firstDayOfWeek: 6 }),
          defaultDate: new Date(),
          onReady: function(_, __, inst){
            inst.calendarContainer.style.zIndex = 200000;
            inst.calendarContainer.dir = 'rtl';
          }
        });
        // Ensure the calendar icon opens the date picker
        const openEditCalendarBtn = document.getElementById('openEditCalendar');
        if (openEditCalendarBtn) {
          openEditCalendarBtn.addEventListener('click', () => {
            if (picker) picker.open();
          });
        }
      });

      // مثال تحقق بسيط عند الحفظ
      document.getElementById('editVoucherForm').addEventListener('submit', function (e) {
        e.preventDefault();
        if (!this.checkValidity()) return this.reportValidity();
        alert('تم حفظ التعديلات (نموذج توضيحي)');
        bootstrap.Modal.getInstance(modal).hide();
      });
    })();
    })();
  });
  
  // Datatables js
  // Remove the incorrect script tags and duplicate DataTables initialization
  // The DataTables is already initialized with the table
});
</script>
}